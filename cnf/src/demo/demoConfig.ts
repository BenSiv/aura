/**
 * Aura Configuration
 * Toggle these flags to switch between real mesh networking and demo mode.
 * On main branch, IS_DEMO_MODE should be false.
 */

export const DEMO_CONFIG = {
  IS_DEMO_MODE: true,
  FORCE_RESET_ON_LAUNCH: true,
  INSTANT_MATCH_ON_LIKE: true,
  MUTUAL_MATCH_DEMO: true,
  BYPASS_DB_PERSISTENCE: true,
  USE_SEED_PROFILES: true,
};
